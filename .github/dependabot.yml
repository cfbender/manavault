version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current (Dependabot bumps the pin +
  # updates the trailing version comment when a new release ships).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
